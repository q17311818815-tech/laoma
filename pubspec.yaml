name: laoma_tingshu
description: In-App Browser for Audio Books
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  # 关键：这个插件能让网页直接在 App 里面显示
  webview_flutter: ^4.2.2 
flutter:
  uses-material-design: true
