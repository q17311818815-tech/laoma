name: laoma_tingshu
description: Pro Audio Book App with Floating Player
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.1.1
  html: ^0.15.3
  webview_flutter: ^4.2.2
  # 关键：用来存收藏夹和历史记录
  shared_preferences: ^2.1.0 
  # 关键：精美图标
  cupertino_icons: ^1.0.5

flutter:
  uses-material-design: true
